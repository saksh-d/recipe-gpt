# Core libraries
torch>=2.0.0
transformers>=4.40.0
datasets>=2.18.0
evaluate>=0.4.1

# Optional performance boost (used internally by Hugging Face)
accelerate>=0.27.0

# UI for generation (Phase 5)
gradio>=4.27.0

# Jupyter support (if you're working in notebooks)
ipykernel>=6.0.0